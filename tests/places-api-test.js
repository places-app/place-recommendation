const chai = require('chai');
const expect = chai.expect;
chai.should();
let request = require('supertest');

//request = request(appUrl);

describe('Get Google Place ID from Geocode API', () => {
  it('should return Google Place ID', () => {


    // expect().to.equal();
  });
});
